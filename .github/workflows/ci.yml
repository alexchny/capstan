name: ci

on:
  push:
    branches: [ main ]
  pull_request:
  schedule:
    - cron: "0 6 * * *"

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: pipx install uv

      - name: Install project (dev)
        run: |
          uv venv
          uv pip install -e ".[dev]"

      - name: Black (check only)
        run: uv run black --check .

      - name: Ruff
        run: uv run ruff check src tests

      - name: Mypy
        run: uv run mypy --config-file mypy.ini src

      - name: Tests
        run: uv run pytest --cov=src --cov-report=term-missing

  chaos-nightly:
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pipx install uv
      - run: |
          uv venv
          uv pip install -e ".[dev]"
      - name: Chaos placeholder
        run: echo "TODO: run chaos suite verifying kill-switch <10s" 